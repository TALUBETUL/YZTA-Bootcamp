# Copy this file to .env and replace the placeholder with your Groq API key.
GROQ_API_KEY=gsk_your_api_key_here

# Optional bearer token used only when an approved record is explicitly sent
# from the Retention Operations page to an HTTPS CRM webhook.
CRM_WEBHOOK_TOKEN=your_crm_webhook_token
